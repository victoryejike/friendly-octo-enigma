const initialState = {
  users: ['No User!']
}

const homePageReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default homePageReducer;